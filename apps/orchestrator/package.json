{
  "name": "@catalyst/orchestrator-service",
  "main": "./src/index.ts",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run --exclude '**/*{integration,container}*' --passWithNoTests",
    "test:integration": "vitest run --passWithNoTests container integration"
  },
  "dependencies": {
    "@catalyst/auth-service": "workspace:*",
    "@catalyst/authorization": "workspace:*",
    "@catalyst/config": "workspace:*",
    "@catalyst/envoy-service": "workspace:*",
    "@catalyst/routing": "workspace:*",
    "@catalyst/service": "workspace:*",
    "@catalyst/telemetry": "workspace:*",
    "@cedar-policy/cedar-wasm": "catalog:",
    "@hono/capnweb": "catalog:",
    "better-sqlite3": "catalog:",
    "capnweb": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:dev",
    "testcontainers": "catalog:testing",
    "typescript": "catalog:dev",
    "vitest": "catalog:testing"
  }
}
