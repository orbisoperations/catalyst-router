// Auth service types now live in @catalyst/authorization
// Re-exported here for backward compatibility
export { AuthRpcServer, AuthService, createAuthRpcHandler } from '@catalyst/authorization'
export { systemToken } from './server.js'
